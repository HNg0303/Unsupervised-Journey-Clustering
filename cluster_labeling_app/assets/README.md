# Bundled source snapshots

Read-only snapshot dùng để seed `db.sqlite` lần đầu và hiển thị raw evidence. App không ghi đè các
file này; mọi thay đổi nghiệp vụ được lưu trong SQLite.

- Taxonomy và cluster mapping: `output/scores/taxonomy_naming/`
- Shareholder catalogs: taxonomy-named July score outputs cho Android và iOS
- Raw catalog/ngrams: July partitioned training run
